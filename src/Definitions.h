#ifndef DEFINITIONS_H_INCLUDED
#define DEFINITIONS_H_INCLUDED

#include <cstdint>

using Byte = std::uint16_t;
//bytes are the basic storage unit and have to be able to store the pointer size,
//and the ram has a 16-bit address size,
//so they need to be 16 bits

#endif // DEFINITIONS_H_INCLUDED
